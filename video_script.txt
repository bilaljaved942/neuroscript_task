========================================================================
LOOM VIDEO SCRIPT - NATURAL HEROES PRODUCT PAGE SUBMISSION
========================================================================

Use this script as a guide during your Loom screen recording. Keep your tone relaxed, professional, and conversational.

---

[PART 1: INTRO & LIVE UI DEMO (Show browser screen at http://localhost:5173/)]

"Hi everyone, my name is [Your Name], and this is a walkthrough of my implementation for the full-stack landing page hiring task. 

I decided to build the Natural Heroes 'No BS Deo' product page matching the provided Figma design. I used React, TypeScript, and Vite for the frontend, and Python's FastAPI for the backend.

Let's look at the UI and interactivity:
- At the top, we have the black announcement ribbon, the beige benefits bar, and the main store header with search and action icons. 
- Below that, the navigation menu and breadcrumbs bar are styled in white. On mobile devices, the menu becomes swipeable horizontally so it fits cleanly on one line.
- In the left column, we have our product image gallery. I can click these thumbnails underneath to change the main image, or use these side arrows to scroll through the slideshow.
- In the right column, we can see the review ratings, product titles, and a custom volume selector dropdown. Selecting different volumes dynamically updates the pricing details.
- Underneath, we have the subscription options. When I select a plan card, it highlights and reveals the specific inclusions and welcome kit gifts.
- If I change the quantity widget here, the price on the main checkout button recalculates in real-time. 
- At the bottom, we have the custom trust circles and checklist."

---

[PART 2: RESPONSIVENESS DEMO (Open Chrome Developer Tools and resize the screen)]

"If I open the developer tools and resize the viewport to tablet or mobile, you can see the layout adapts. The columns stack cleanly, the margins adjust, the trust circles wrap to a 2x2 grid, and the counter widget, checkout button, and wishlist heart align neatly in a single horizontal row without any squishing or overflow."

---

[PART 3: CODEBASE WALKTHROUGH (Show VS Code editor)]

"Now let's take a quick look at the codebase structure. 

The project is divided into two folders:
- Under 'frontend', we have the React components. I used Vanilla CSS modules to style the layout, which keeps the build size very small and fast. I also created a TypeScript interface in 'types.ts' to ensure all content fetched from the API is strongly typed.
- Under 'backend', we have the FastAPI app. 'app.py' serves the content configuration through a GET endpoint, and handles updates through a PUT endpoint. 
- I also configured a root '.gitignore' to keep the repository clean of node modules, build targets, and python virtual environments."

---

[PART 4: ZERO-REDEPLOYMENT UPDATE DEMO (Show VS Code with backend/content.json, and the browser side-by-side)]

"Now let me show you how the page content can be updated dynamically without redeploying the site. 

If I open 'backend/content.json' in VS Code, we can find the fields for our e-commerce copy. Let's find the main product title 'No BS Deo'. 

I will change this title to 'Organic Fresh Deo' and save the file. 

If I return to my browser and refresh the page, you can see the product title immediately updates to 'Organic Fresh Deo'. 

The application fetches the latest content structure from the FastAPI server on load. If someone updates the JSON configuration on the server (or triggers a PUT API call to it), the frontend receives the new data instantly, achieving dynamic edits without rebuilds or deployments. I will restore the name back to 'No BS Deo' now."

---

[PART 5: DOCKER SETUP & OUTRO (Show Docker files in VS Code)]

"Lastly, for easy setup, I've containerized the entire app. I created a multi-stage Dockerfile that builds the React project and mounts the output statically inside the FastAPI container, which serves the entire site on port 8000. Anyone can launch the app locally using 'docker-compose up --build'.

Thanks for watching, and I look forward to your feedback!"
