version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    volumes:
      # Optional: Persist e-commerce content edits (content.json) on disk inside the container
      - content-data:/app/backend
    restart: unless-stopped

volumes:
  content-data:
